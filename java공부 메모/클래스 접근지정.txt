클래스 접근지정

public 
public 클래스로 선언하면 다른 어떤 클래스에서도 객체를 생성할수있다.

public class World{ public 클래스

}

default
접근지정자 없이 클래스를 선언한 경우, 디폴트(default) 접근 지정으로 선언되었다고 한다.
디폴트로 선언된 클래스는 오직 같은 패키지 내에 있는 클래스들에게만 사용이 가능하다.

class World { difault 클래스

}



