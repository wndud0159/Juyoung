REPLACE() 구문
REPLACER() 함수는 문자열의 대상문자를 교체문자로 바꾼다

REPLACE(문자열,대상문자,교체문자)
결괏값 문자

사용 예
REPLACE('2019/08/20','/','')
2019/08/20에서 /를 로 바꿔서 문자열을 만든다


20190820 문자열에서 문자를 공백으로 바꾼다
SELECT REPLACE('20190820', '', '')
FROM DUAL;
