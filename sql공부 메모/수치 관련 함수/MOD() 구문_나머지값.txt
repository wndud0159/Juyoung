MOD() 구문
MOD() 함수는 수치를 나누기 값으로 나눈 후 나머지 값을 구한다.
예를들어 MOD(10.3)의 결과는 10을 3으로 나눈 나머지 값인 1이다

MOD(수치, 나머지값)
결괏값 수치

사용 예
MOD(8, 3)
8을 3으로 나눈 후 나머지 값인 2를 구한다


4/2, 9/5, 12/0, 13/4 나머지 값을 구한다
SELECT MOD(4, 2),
MOD(9, 5),
MOD(12, 0),
MOD(13, 4)
FROM DUAL;