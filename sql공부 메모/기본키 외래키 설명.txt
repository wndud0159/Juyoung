 기본키(PRIMARY KEY)
 테이블에서 레코드의 중복을 허용하지 않는 대표성을 가진 유일한 필드의 모음
 기본키 필드 값은 중복되면 안되지만 기본키 필드를 제외한 값은 중복돼도 상관없다.

 참조키or외래키(FOREIGN KEY)
 테이블 간에 관계를 나타내는 필드의 모음 
 참조키는 여러 관련 테이블을 조작하면서 규칙에 맞게 데이터를 입력, 수정, 삭제하게 하는 것으로
 부정합데이터를 줄이는 역할을 한다.

